# Personal rule overrides (see rules/*.local.example.md)
rules/antipatterns.local.md
rules/banned_words.local.md

# Per-project voice profile and other clearfelt state
.clearfelt/

# Opt-in saved artifacts (audit reports, eval trend data), never a default write target
reports/

# Machine-local Claude Code settings (hook installs live here)
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*

# Editor/OS cruft
.DS_Store
*.swp
